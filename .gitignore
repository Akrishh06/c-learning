# Only track source/docs under work/; compiled binaries have no fixed
# extension so we whitelist source file types instead of blacklisting.
work/**
!work/**/
!work/**/*.c
!work/**/*.h
!work/**/*.md
*.o
*.out
